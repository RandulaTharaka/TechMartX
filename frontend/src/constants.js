/* export const BASE_URL =
  process.env.NODE_ENV === "development" ? "http://localhost:5000" : ""; */ // implement this lines of code, if proxy isn't used

export const BASE_URL = ""; // empty "" because proxy is in used
export const PRODUCTS_URL = "/api/products";
export const USERS_URL = "/api/users";
export const ODERS_URL = "/api/orders";
export const PAYPAL_URL = "/api/config/paypal";
